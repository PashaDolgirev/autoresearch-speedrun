numpy
tqdm
# torch 2.6 fails to compile FlexAttention's backward kernel and torch 2.7.0
# hits a non-deterministic NCCL deadlock on 8x A100 + NCCL 2.26.2. 2.7.1+cu126
# is the version actually verified to run record-18 cleanly on this hardware.
torch==2.7.1
huggingface-hub
